給自己的提醒:

1.bun install

2.啟動步驟:到backend在terminal安裝pip install -r requirements.txt(為了讓前後端連動)。後端導入ocr&llm

3.啟動指令:到frontend/bun run start:all

TODO_LIST:1.在llm跟ocr裡面放入真的程式而非假資料(已完成)
          2.把解析的文字不要顯示在前端頁面
